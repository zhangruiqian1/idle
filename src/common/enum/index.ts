export enum RoleType {
    Hero = 'Hero',
    Enemy = 'Enemy'
}

export enum RoleState {
    Idle = 'Idle',
    Attack = 'Attack',
    Hurt = 'Hurt',
    Recover = 'Recover',
    Dead = 'Dead',
}

export enum FightMode {
    Push = '推图',
    Loop = '循环'
}